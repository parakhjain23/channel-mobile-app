const initialState = {
  user: null,
  guest: false,
  isLoading: true,
};

export function userInfoReducer(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}
