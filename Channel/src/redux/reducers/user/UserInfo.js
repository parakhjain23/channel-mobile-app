const initialState = {
  user: null,
  guest: false,
  isLoading: true,
  isAuthticated: false,
  signedIn: false,
};

export function userInfoReducer(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}
